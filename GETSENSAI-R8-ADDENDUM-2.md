# getsensai.co — Round 8, second addendum: two lines

Your R8 addendum report was read in full; the rings measurement was right and the crop was ours.

1. **The rings still is re-cut** and committed on `main` (`0c886f4`,
   `public/screenshots/film/leak-fraud-rings-graph.webp`, 1142×1396, whole network in frame, hub
   centred, no money on it). Nothing in the component changes; push it.

2. **Phone ink cards: symmetric bleed.** AA's call on your open question. Keep the width reclaim,
   but centre it: the card bleeds the same amount left and right of the row's text column, so it
   sits centred under the text at 360 and 390 instead of 56px left / 34px right. Same for all five
   rows. Check the first row at 390 before pushing.

Commit on `main` as `Round 8: rings still, symmetric ink cards`. Production after AA sees it on his
phone; the preview is behind Vercel's login again, so tell him when it's live.
