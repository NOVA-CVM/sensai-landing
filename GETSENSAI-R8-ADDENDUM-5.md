# getsensai.co — Round 8, fifth addendum: the pattern library, said out loud

Two copy changes, same deploy as addendum 4 (film v29). AA's point: a CRO should read on this page
that sensAi arrives already knowing the patterns, on day one, and that his team builds nothing.

## 1. "What changes for you" gets a fourth line

`OutcomesStrip` goes from three items to four. The fourth, last:

**Dozens of known gaming patterns watching your base from day one.**

Mark: a stacked library, three horizontal bars offset like a small stack of cards, in the same
stroke and size as the three existing marks (`MarkOffer`, `MarkChurn`, `MarkFailure`); call it
`MarkLibrary`. Layout: four across on desktop if the type still holds at 17px semibold with no
line breaking mid-phrase; otherwise two rows of two. Stacked on phones, as now. Eyebrow and
everything else in the strip unchanged.

## 2. The Learn tile says the same thing

The tile's line becomes:

*Starts with dozens of known gaming patterns, built over years of operator work. sensAi finds new
ones and proposes them; once your team approves, they're added.*

Three lines at 390 is fine. Mark unchanged. The other three tiles unchanged.

## Words that stay out

No client or operator names, no "AI", "machine learning", "models" or "agents" around this; the
claim is the library and the years, nothing else. Don't add a number anywhere except "dozens".

## Done means

- Four outcome lines, the fourth exactly as above, no mid-phrase break at 390 or 1440.
- The Learn tile line exactly as above.
- Copy audit words from addendum 1 still all absent from the served page.
- `tsc --noEmit` and `next build` clean. Commit on `main` as `Round 8: the pattern library`,
  deployed together with the v29 film (addendum 4). Live URL in the report.
